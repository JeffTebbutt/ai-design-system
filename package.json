{
  "name": "ai-design-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/figma-to-contract.js && node scripts/contract-to-component.js",
    "check": "node scripts/check-drift.js",
    "build": "npm run generate && npm run check"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7"
  }
}
